name: flutter_application_2
description: Habit Tracking App with Firebase
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.4.0
  cloud_firestore: ^5.4.0

  # Fonts (Noto – Urdu/English safe)
  google_fonts: ^6.1.0

flutter:
  uses-material-design: true
